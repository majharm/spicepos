Production deploy bundle — 20260830deploy12
=========================================

Upload ALL files below to Hostinger public_html (overwrite):

Assets
  assets/atav-telecom-logo.png

HTML
  index.html
  login.html
  master.html

CSS
  css/pos.css
  css/saas.css

JavaScript
  js/app.js
  js/invoice.js
  js/dev-mode.js
  js/master.js
  js/pos-api.js
  js/x-pos-20260830e.js

PHP
  pos-php-core.php
  pos-php-till.php
  pos-reports.php
  pos-api.php
  api/health/index.php
  health.json

Node (if using hPanel Node.js web app — upload OR git pull then Restart)
  server.js
  server/auth.js
  server/master.js
  server/onboard.js
  server/index.js
  server/reports.js
  package.json

After upload:
  1. hPanel → Node.js → Restart (if Node app enabled)
  2. Hard-refresh browser (Ctrl+Shift+R)

FIXES in this bundle
  - Thermal tax invoice print for sales orders (80mm, GST breakdown, HSN, CGST/SGST)
  - Sales orders renamed to Invoices in POS nav
  - GST reports: output/input by rate, HSN itemwise, B2B/B2C sales, net GST summary
  - Full PHP reports API (not just summary) for production without Node
  - Excel export for all GST sheets (PHP + Node)
  - Hide master admin viewing banner in business POS (keep Back to Master Admin in nav)
  - HOTFIX: restore pos_send() in pos-php-core.php (fixes /api/health 500)
  - Stop showing auto "Master admin login" notice in business POS
  - Master admin impersonation works without visible shop banner
  - Platform notifications show on business dashboard (manual master notices only)
  - auth/me prefers active staff session over master cookie (business login fix)
  - Master admin Open POS per user (Users tab)
  - Business password change on edit (PHP + date normalization)
  - Exit impersonation without full logout
  - Master admin panel auto-exits shop view when opened during impersonation
  - Removed conflicting x-pos script from master.html / index.html

How to use Open POS
  1. Sign in at /master.html
  2. Businesses → Open POS (enters as business admin)
     OR Users → Open POS (enters as that specific user)
  3. Shop POS opens with full access (even if expired/suspended)
  4. Back to Master Admin ends shop view only
